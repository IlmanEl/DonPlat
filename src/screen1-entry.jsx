import React from 'react';
import ReactDOM from 'react-dom/client';
import ProjectsUnified from './app-unified.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ProjectsUnified />
  </React.StrictMode>
);
